import { PaginationProps } from '@mui/material/Pagination';

const paginationStyle: PaginationProps = {
  color: 'primary',
  defaultPage: 1,
  siblingCount: 0,
  boundaryCount: 0,
};

export { paginationStyle };
