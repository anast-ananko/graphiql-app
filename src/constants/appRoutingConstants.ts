const _appPaths = {
  ROOT: '/',
  MAIN: '/main',
  LOGIN: '/sign-in',
  SIGN_IN: '/sign-in',
  SIGN_UP: '/sign-up',
};

const APP_ROUTE_PATHS = Object.freeze(_appPaths);

export { APP_ROUTE_PATHS };
